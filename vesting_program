#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
JAR="$SCRIPT_DIR/target/vesting-program-1.0.0.jar"

if [ ! -f "$JAR" ]; then
    echo "Building project (first run)..." >&2
    "$SCRIPT_DIR/mvnw" -q clean package -DskipTests || exit 1
fi

java -jar "$JAR" "$@"
